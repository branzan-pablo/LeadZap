/**
 * Persist push subscription for a user (implement with Supabase in Phase 2+).
 */
export async function savePushSubscription(
  _userId: string,
  _subscription: { endpoint: string; keys: { p256dh: string; auth: string } }
) {
  //
}
